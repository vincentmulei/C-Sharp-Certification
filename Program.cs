using System;
using part1;  // Import the namespace where HelloWorld lives

var myFirstCode = new MyFirstCode();  // Create an instance of HelloWorld
//myFirstCode.HelloWorld();  // Call the HelloWorld method to print the message


var literals = new Literals();  // Create an instance of Literals

//literals.StringLiterals();  // Call the StringLiterals method to print the literal values
//literals.IntegerLiterals();  // Call the IntegerLiterals method to print the literal values
//literals.FloatingPointLiterals();  // Call the FloatingPointLiterals method to print the literal values
//literals.BooleanLiterals();  // Call the BooleanLiterals method to print the literal values
//literals.ImplicitlyTypedLocalVariables();  // Call the ImplicitlyTypedLocalVariables method to print the variable values
    
var stringFormatting = new StringFormatting();  // Create an instance of 

//stringFormatting.CharacterEscapes();  // Call the CharacterEscapes method to print the formatted string
stringFormatting.VerbatimStringLiterals();  // Call the VerbatimStringLiterals method to print the verbatim string literal